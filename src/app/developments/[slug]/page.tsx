import type { Metadata } from "next";
import Image from "next/image";
import Link from "next/link";
import { ArrowUpRight } from "lucide-react";
import { notFound } from "next/navigation";
import { Button } from "@/components/ui/button";
import { JsonLd } from "@/components/seo-json-ld";
import { Callout, ImagePanel, SectionHeading, SiteFooter, SiteHeader } from "@/components/freco-site";
import { ProjectCard } from "@/components/freco-content";
import { projects as defaultProjects } from "@/lib/freco-content";
import { getProjects } from "@/lib/site-content";
import { siteUrl } from "@/lib/site";

export function generateStaticParams() { return defaultProjects.map((project) => ({ slug: project.slug })); }

export async function generateMetadata({ params }: { params: Promise<{ slug: string }> }): Promise<Metadata> {
  const { slug } = await params;
  const project = (await getProjects()).find((item) => item.slug === slug);
  if (!project) return {};
  return { title: `${project.name} | ${project.category} Development Concept`, description: `${project.description} Explore the development concept, visual direction and current information for ${project.name}.`, keywords: [project.name, `${project.category} development Kenya`, "Kenya property development", "FRECO ART"], alternates: { canonical: `/developments/${project.slug}` }, openGraph: { type: "article", title: `${project.name} | FRECO ART LTD`, description: project.description, images: [{ url: project.featuredImage, alt: `${project.name} development concept` }] } };
}

export default async function ProjectPage({ params }: { params: Promise<{ slug: string }> }) {
  const { slug } = await params;
  const projects = await getProjects();
  const project = projects.find((item) => item.slug === slug);
  if (!project) notFound();
  const related = projects.filter((item) => item.id !== project.id).slice(0, 2);
  const baseUrl = await siteUrl();
  return <main><JsonLd data={{ "@context": "https://schema.org", "@type": "WebPage", name: project.name, description: project.description, url: `${baseUrl}/developments/${project.slug}`, isPartOf: { "@type": "WebSite", name: "FRECO ART LTD", url: baseUrl }, about: { "@type": "Organization", name: "FRECO ART LTD" } }} /><JsonLd data={{ "@context": "https://schema.org", "@type": "BreadcrumbList", itemListElement: [{ "@type": "ListItem", position: 1, name: "Home", item: baseUrl }, { "@type": "ListItem", position: 2, name: "Developments", item: `${baseUrl}/developments` }, { "@type": "ListItem", position: 3, name: project.name, item: `${baseUrl}/developments/${project.slug}` }] }} /><section className="relative min-h-[680px] overflow-hidden bg-ink text-white"><Image src={project.featuredImage} alt={`${project.name} development concept`} fill priority className="object-cover opacity-65" sizes="100vw" /><div className="absolute inset-0 bg-gradient-to-r from-ink via-ink/65 to-transparent" /><SiteHeader /><div className="relative mx-auto flex min-h-[680px] max-w-[1440px] items-end px-6 pb-20 pt-40 lg:px-12 lg:pb-28"><div><p className="eyebrow text-white/55">{project.category} · {project.status}</p><h1 className="mt-6 max-w-4xl text-6xl font-semibold leading-[0.92] tracking-[-0.08em] sm:text-8xl">{project.name}</h1><p className="mt-6 text-sm uppercase tracking-[0.15em] text-white/60">{project.location}</p></div></div></section><section className="bg-background"><div className="mx-auto grid max-w-[1440px] gap-14 px-6 py-24 lg:grid-cols-[0.8fr_1.2fr] lg:px-12 lg:py-32"><SectionHeading eyebrow="The development" title="A place shaped by context." copy={project.description} /><div className="space-y-8 text-base leading-7 text-muted-foreground"><p>{project.opportunity}</p><Button render={<Link href="/contact" />} className="h-12 rounded-full bg-primary px-6 text-[11px] font-semibold uppercase tracking-[0.16em] text-white hover:bg-primary/90">Register interest <ArrowUpRight size={15} /></Button></div></div></section><section className="bg-sand"><div className="mx-auto max-w-[1440px] px-6 py-24 lg:px-12 lg:py-32"><SectionHeading eyebrow="Development details" title="The information will grow with the project." /><div className="mt-14 grid border-l border-t border-border sm:grid-cols-2 lg:grid-cols-4">{[["Location", project.location], ["Type", project.category], ["Status", project.status], ["Scale", project.scale]].map(([label, value]) => <div key={label} className="border-b border-r border-border p-6"><p className="eyebrow text-primary">{label}</p><p className="mt-8 text-sm leading-6 text-muted-foreground">{value}</p></div>)}</div><div className="mt-14 grid gap-5 md:grid-cols-2">{project.amenities.map((amenity) => <div key={amenity} className="border-t border-border pt-4 text-sm text-muted-foreground">{amenity}</div>)}</div></div></section><section className="bg-background"><div className="mx-auto max-w-[1440px] px-6 py-24 lg:px-12 lg:py-32"><SectionHeading eyebrow="Gallery" title="A visual direction for the opportunity." copy="These visuals are concept references. Verified project imagery and details will replace them as each development progresses." /><div className="mt-14 grid gap-5 md:grid-cols-2">{project.gallery.map((image, index) => <ImagePanel key={`${image}-${index}`} src={image} alt={`${project.name} concept visual ${index + 1}`} className={index === 0 ? "aspect-[1.25/1] md:row-span-2 md:aspect-auto" : "aspect-[1.25/1]"} />)}</div></div></section><section className="bg-sand"><div className="mx-auto max-w-[1440px] px-6 py-24 lg:px-12 lg:py-32"><div className="grid gap-12 lg:grid-cols-[1fr_1fr]"><div><p className="eyebrow text-primary">Location</p><h2 className="mt-5 text-4xl font-semibold tracking-[-0.06em]">The map belongs here when the location is verified.</h2></div><div className="grid min-h-[260px] place-items-center border border-border bg-white text-center"><div><p className="eyebrow text-primary">Map placeholder</p><p className="mt-3 text-sm text-muted-foreground">Coordinates to be provided.</p></div></div></div></div></section><section className="bg-background"><div className="mx-auto max-w-[1440px] px-6 py-24 lg:px-12 lg:py-32"><SectionHeading eyebrow="Related developments" title="More from the portfolio." /><div className="mt-14 grid gap-10 md:grid-cols-2">{related.map((item) => <ProjectCard key={item.id} project={item} />)}</div></div></section><Callout eyebrow="Enquiry" title="Interested in this development?" copy="Tell us what you want to understand and we will route the conversation appropriately." href="/contact" label="Register interest" /><SiteFooter /></main>;
}
